---
date: 2026-09-13
generated_by: claude
---

Eighth run in a row with partial coverage. The egress proxy still refuses every blog, newsletter and video host on the follow list at CONNECT — simonwillison.net, marginalrevolution.com, lucumr.pocoo.org, world.hey.com, astralcodexten.com, overcomingbias.com, deeplearning.ai, addyosmani.com, mitchellh.com, blog.getzep.com, youtube.com and x.com — and today the block also covers en.wikipedia.org, news.ycombinator.com and every news domain tried, so the news-about-them pass has nothing it can verify. Reachable: github.com over the git protocol, raw.githubusercontent.com, registry.npmjs.org and pypi.org. The GitHub REST API is now restricted to this repository, so user-activity endpoints no longer work; everything below was read out of a cloned repository or the npm registry.

## Simon Willison
- **An XSS hole in the blog's tag-autocomplete debug view gets closed** (github, 2026-09-11) [link](https://github.com/simonw/simonwillisonblog/commit/21e1a55123ef5ce8a03c1f561e732725d232086a)
  `/tags-autocomplete/?debug=1` built its response by string concatenation — `"<html><body><pre>" + json.dumps(...) + "</pre><hr><code>" + str(tags.query)` — so both the JSON dump of matching tags and the rendered SQL, which embeds the user's `q` parameter, reached the browser unescaped. It now goes through `django.utils.html.format_html` with two placeholders, and the previously unclosed `</code>` is closed. Two tests pin it: one sends `<script>alert(1)</script>` and `</code><svg/onload=alert(1)>` as `q` and asserts the escaped forms appear while the raw ones do not; the other puts `</pre><script>alert(1)</script>` in a tag description and checks the debug HTML escapes it while the plain JSON endpoint still returns it verbatim.

  Reflected XSS in a debug branch of a public endpoint — nothing gates `?debug=1` behind auth. It lands the day after Datasette 1.0a39 shipped its own batch of HTML and SQL-identifier escaping fixes for untrusted schema names, so that is two separate codebases of his hardened against the same class of bug inside a week.

- **Draft guide revisions stop leaking through chapter history and the sitemap** (github, 2026-09-11) [link](https://github.com/simonw/simonwillisonblog/commit/ee1f5948f73f1acc631ff0aa332d5a2f7dac6b53)
  `ChapterChange` recorded a chapter's own `is_draft` but nothing about the guide it belonged to, so once a guide was published every historical revision — including ones written while the whole guide was still private — became readable on the public chapter-changes diff page. Migration 0004 adds a nullable `guide_is_draft`; existing rows stay NULL, which the model comment defines as unknown visibility and therefore staff-only. A new `_visible_chapter_changes()` filters `is_draft=False, guide_is_draft=False` for non-staff and feeds both the diff list and the aggregate behind the change count and the first/last-modified dates. The sitemap additionally filters `Chapter` rows to `guide__is_draft=False`.

  `Chapter.save()` was rewritten because the fix depends on reading the guide correctly: it re-fetches the saved row with `select_related("guide")` before snapshotting, since the in-memory instance can hold unsaved fields or a stale cached guide, and `guide_id` joins the set of fields that trigger a new revision. Staff responses are now always marked no-cache. 189 lines of new tests.

## Boris Cherny
- **Claude Code 2.1.270 is a one-line release undoing a permission regression in 2.1.269** (github, 2026-09-12) [link](https://github.com/anthropics/claude-code/blob/v2.1.270/CHANGELOG.md)
  Published to npm at 18:52 UTC and tagged `v2.1.270` in the repo at 19:45, taking both `latest` and `next`; `stable` has not moved off 2.1.236 all week. The changelog carries exactly one entry: read-only git commands in Bash were unexpectedly asking for permission after a session had been running for a while, described there as a regression in 2.1.269. Nothing else shipped.

  2.1.269 landed the day before with roughly a hundred entries, three of which reworked permission scoping — `!`-prefixed deny and ask rules confined to the settings source that wrote them, and `Edit()` deny rules extended to the file a Bash `tee` writes. A same-day single-fix release pointing back at that work suggests the git-command prompts came out of it, but the changelog does not say what the mechanism was, only that the symptom appeared partway through a session rather than at startup.

## Mitchell Hashimoto
- **Ghostty funnels all of its C-header translation through one new build package** (github, 2026-09-12) [link](https://github.com/ghostty-org/ghostty/commit/e2e53f861482e080bf45054ba49ef471f9849937)
  PR #14203, merged 21:22 Pacific on 11 September (04:22 UTC on the 12th). It adds `pkg/translate-c` — 218 lines of `build.zig` plus a manifest — as an internal wrapper around the external translate-c library, so the dependency is declared in one package instead of spreading across `pkg/` as more dependencies migrate. The commit message names the destination: a "larger removal of `cImport`" and better coordination between building C dependencies and translating their headers, replacing an imperative style with a declarative, data-driven description of what gets translated. `pkg/harfbuzz/build.zig` absorbs most of the churn at 452 lines changed, its individual settings pulled into helpers shared between translation and the static-library build; Wuffs stops generating a file full of macro defines and passes them as `-D` flags instead.

  13 files, +590/−508, including 125 changed lines in `src/build/SharedDeps.zig`. Build plumbing with no user-visible effect — Ghostty's last tagged release is still v1.3.1 from March, and only the rolling `tip` tag moved, on 12 September.

## Armin Ronacher
- **P(doom)** (blog, 2026-09-12) [link](https://github.com/mitsuhiko/lucumr/blob/e6560f874078202daca48119af805259d54435b2/blog/posts/2026/09-12-pdoom.md)
  Ronacher's answer to Dario Amodei's "we must pace the frontier": he says he shares the observations and largely the concerns, and opposes the conclusion. Three objections. Pacing concerns exactly two companies, Anthropic and OpenAI, from the same origin, and the proposed third-party evaluator METR has ties to both — so a technology trained on the public commons ends up governed by a handful of American corporations. The models causing real damage today are closed-weight American ones, and open weights would supply their own pacing, which he calls the truest form of mutually assured destruction. And Chinese labs distilling American models are, in his phrasing, "massively bailing out the world", especially for Europeans.

  He does not expect extinction. He expects cost: a new tax paid to model providers, both to keep up and to deal with "these machines finding security issues left and right", with software engineering the early victim and universities and research groups next. On regulation he is scathing in both directions — EU rules two years old and aimed at problems nobody has, the US "turbo capitalism paired with sinophobia". Committed to the blog's source repo at 19:54 UTC; lucumr.pocoo.org is unreachable from this run, so this is read from the post file at that commit, not from the published page.
