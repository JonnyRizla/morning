---
date: 2026-09-15
generated_by: claude
---

Tenth run with partial coverage. The egress proxy still answers 403 to CONNECT for every blog, newsletter and video host on the follow list — simonwillison.net, marginalrevolution.com, astralcodexten.com, overcomingbias.com, world.hey.com, lucumr.pocoo.org, deeplearning.ai, addyosmani.com, mitchellh.com, blog.getzep.com, youtube.com, x.com — and also for github.com web pages and api.github.com, which today returns a scope error naming this repository. Reachable: the git protocol to github.com, raw.githubusercontent.com, registry.npmjs.org, pypi.org (both the JSON API and project pages), and web search. Everything below was read out of a cloned repository, an npm tarball or the PyPI API. Search ran as the news-about-them pass for everyone who was quiet and returned nothing datable to the window that could be verified without opening a page, so it produced no items — it is an index, not a reader. Simon Willison's blog item comes from the public Postgres dump in `simonw/simonwillisonblog-backup`; his two releases come from PyPI and the source repositories.

## Simon Willison
- **commit-rewriter 0.1, a local web app for rewriting git commit messages** (pypi, 2026-09-14) [link](https://pypi.org/project/commit-rewriter/0.1/)
  `uvx commit-rewriter /path/to/repository`, or no path for the current directory, starts a server on `127.0.0.1:8000` (`-p/--port` moves it) that lists the repository's commits in a "Navigate commits" sidebar and gives each one a card with its hash, author, timestamp, an editable textarea holding the message and a "View full formatted diff" toggle. A toolbar carries the pending-edit count, Discard drafts and Rewrite commit messages, plus a search box over message, author or hash and an "Edited only" checkbox. Applying the edits creates a backup branch first. Tagged 0.1 at 00:24 UTC; the last two commits before it were "Allow unstaged files, closes #2" and a move to a proper package with the HTML and JS bundled in.

  The repository is five days old — first commit 9 September, "Add commit-rewriter CLI, web editor, tests, and CI". Its README screenshot is a `.webp`, generated by `shot-scraper multi shots.yml`, which is the reason for the item below: he added WebP output to shot-scraper 36 minutes before tagging this.

- **shot-scraper 1.12 adds WebP output and an explicit `--format`** (github, 2026-09-13) [link](https://github.com/simonw/shot-scraper/commit/d457df57f5b930e11bfb7a04c7059500753cf483)
  An `-o` filename ending in `.webp` now produces WebP, and a new `--format png|jpeg|webp` overrides the extension entirely — which also makes formats work with `-o -`, as in `shot-scraper https://datasette.io/ --format webp -o - > datasette.webp`. WebP is lossless by default, equivalent to `--quality 100`; `--quality` takes 0–100 and now only implies JPEG when WebP has not been selected, and combining `--format png` with `--quality` is a hard error because PNG has no quality setting. `--omit-background` transparency, which previously broke whenever `--quality` was set, works with WebP at a quality. The docs note WebP's 16,383-pixel per-dimension limit and tell you to pass `--height` or a selector on longer pages. 195 lines of new tests in `tests/test_image_formats.py`.

  Committed 16:48 Pacific, released as 1.12 nine minutes later and on PyPI at 00:00 UTC. The same options landed in the `multi` YAML format as `format:` and `quality:` keys per item.

- **A quote from Laurie Voss** (blog, 2026-09-14) [link](https://simonwillison.net/2026/Sep/14/laurie-voss/)
  The passage Willison pulled from Voss's post "We are all Product Engineers now": "The cost of writing code collapsed, and the cost of reviewing, fixing and operating it is following, and I'm assuming it gets there. What's left of making software is finding out what people actually want, defining it precisely, and making it pleasant to use. That cost is per piece of software and doesn't transfer, so as the amount of software goes to infinity, which it will because there's no ceiling on demand, that cost becomes the whole job."

  Posted 14:34 UTC. Read from the blog's public database dump, not the published page — neither simonwillison.net nor seldo.com was reachable from this run, so this is the quotation as he stored it and nothing about the rest of Voss's argument.

## Boris Cherny
- **Claude Code 2.1.271 caps the Monitor tool's deadline at 30 minutes and drops persistent monitors** (npm, 2026-09-14) [link](https://www.npmjs.com/package/@anthropic-ai/claude-code/v/2.1.271)
  Published at 19:45:19 UTC to the `next` tag only — `latest` is still 2.1.270, and `stable` has not moved off 2.1.236 in two weeks. There is no changelog entry (the CHANGELOG on `main` still tops out at 2.1.270) and no `v2.1.271` git tag, so the only account of what changed is the package: of its seven files, only `sdk-tools.d.ts` differs from 2.1.270, by 38 bytes. The Monitor tool loses its `persistent` boolean, documented in 2.1.270 as running "for the lifetime of the session (no timeout)... for session-length watches like PR monitoring or log tails". Its `deadline` text changes to match, from "Default 300000ms, max 3600000ms. Ignored when persistent is true" to "Default 300000ms. Deadlines above 1800000ms are capped to 1800000ms. You are notified at expiry and can re-arm."

  So an unbounded session-length watch becomes a hard 30-minute ceiling plus an expiry notification you re-arm by hand. The same diff moves the Artifact tool's `read_asset` default output directory from the working directory to the artifact's folder in the session scratchpad, "where saving needs no approval". This is the type-declaration surface only; the compiled binary is not in the tarball, so nothing here says what else shipped.

## Mitchell Hashimoto
- **Ghostty answers the ANSI form of DECRQM, and stops truncating mode numbers** (github, 2026-09-14) [link](https://github.com/ghostty-org/ghostty/commit/9dc0d974e62d4a98f55680a58fa574949f4bf134)
  Only the DEC private form `CSI ? Ps $ p` had been reaching Ghostty's mode-query handler; the ANSI form `CSI Ps $ p`, specified in the VT510 reference and xterm, was dropped on the floor. Both now answer and report independently: after `CSI 4 h` followed by `CSI ? 4 l`, `CSI 4 $ p` replies `CSI 4;1$y` (set) while `CSI ? 4 $ p` replies `CSI ?4;2$y` (reset), and an unrecognised mode replies `;0$y`. 128 lines across `stream.zig`, `stream_terminal.zig` and the C API, with a test that feeds each query one byte-split at a time to prove nothing is emitted until the sequence completes. Filed against issue #14225, merged as #14236.

  Seven minutes later he pushed the follow-up, merged as [#14237](https://github.com/ghostty-org/ghostty/commit/3beb6d717ac305e6a3c7b152103a6f7e81d8e3cf): the report tag packed a u15 mode number plus an ANSI flag into a u16, so a query for mode 32772 aliased ANSI mode 4 and 32885 aliased DEC private mode 117, both answering as if the mode were known. `Report.Tag` is now a separate struct carrying a full u16 and its own bool, kept apart from the packed `ModeTag` the public C API uses.

- **Ghostty's GTK app runtime stops depending on OpenGL entirely** (github, 2026-09-14) [link](https://github.com/ghostty-org/ghostty/commit/49b95d846621469ebc5950fd005fa57bcc160d85)
  PR #14052, authored by Leah Amelia Chen over three weeks and merged by Hashimoto at 12:07 Pacific. GtkGLArea held its own `GdkGLContext` on the main thread, which pinned all of Ghostty's GL calls to the main thread, played badly with triple buffering, and forced a deferred initialisation dance in which the renderer and core surface could not start until the GLArea had its context. It is replaced by a custom widget, `RenderSurface`, that takes DMABUFs and displays them; acquiring a context moves to hand-written EGL bindings in a new 267-line `pkg/opengl/egl.zig`, chosen because EGL is also what exports GL textures into DMABUFs, and that context now lives only on the render thread. The main thread learns about rendering only when a frame is pushed.

  The consequence the PR leads on: the GTK apprt no longer references OpenGL anywhere, so a Vulkan or GLES backend becomes a matter of feeding it DMABUFs. Its AI disclosure says the author had the idea and let Pi implement most of the EGL and renderer replumbing, then "carefully vetted every line" and spent about as long reviewing as coding. Merged in the same batch as [#14212](https://github.com/ghostty-org/ghostty/commit/1ab2501e79b7f1b204896a1009cd6566c93f4e46) from Jeffrey C. Ollie, which stops the GTK blueprint wrapper poisoning the Zig build cache — a rebuild after the wrapper relinks goes from about 43s to about 7s. Ghostty's last tagged release is still v1.3.1.

## Topic: AI agents, harnesses and tooling (Claude Code, MCP, agent skills)
- **Claude Code's `/diff` plugin reaches parity with the built-in diff viewer** (github, 2026-09-14) [link](https://github.com/anthropics/claude-code/commit/f4ceeecabff13e814dc4302620e2da1a688e47f8)
  PR #94184, merged into `anthropics/claude-code` at 14:00 UTC: 280 files, +4,998/−1,432. `mods/diff` reimplements the built-in diff pane as a plugin on the public API, and this round pins the header, file list and toggles while the wheel moves the hunks beneath them three rows a tick — or moves the list one file a tick when the pointer is over it — by answering the pane's `ui.scroll` itself rather than letting the engine scroll the window. It picks up the built-in's chords (`ctrl+up`/`ctrl+down` and `opt+up`/`opt+down` on the list, `ctrl+x b` to advance the comparison base) through Buttons that declare the engine's own actions, and without the fullscreen layout `/diff` now opens an inline dialog at any width: five file rows around the selection, Enter for one file's hunks, Escape back to the list and then closed.

  The revealing number is the type surface it needed. `mods/types/claude-code.d.ts` gains 915 lines in this one PR — `ui.focus`, `ui.scroll`'s pointer, `by` and `contentRows`, `ui.open`'s `rows`, `autoFocus`, `closeOnEscape`, `holdToasts`, and `command.run`'s `presentation`, which is how the plugin learns whether the fullscreen layout is on. The plugin also drops `clock.sleep` from what it calls. This reads less like a sample plugin than like a first-party feature being rebuilt on the public API to find out what the API is still missing.
